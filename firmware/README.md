# Firmware

Main firmware:

```text
jm_nixieclock_v2_custom_firmware.asm
```

Latest generated image:

```text
jm_nixieclock_v2_custom_firmware.s19
```

Most other `test_*` files in this directory were temporary hardware bring-up programs used for measuring the board and are ignored by `.gitignore`.
